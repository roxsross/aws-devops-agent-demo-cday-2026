# Estado y variables locales de Terraform (pueden contener secretos).
# Los artefactos .terraform/ y .terraform.* ya se ignoran en el .gitignore raíz.
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
